% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotate.R
\name{score_cengen_clusters}
\alias{score_cengen_clusters}
\title{Score and classify clusters against CeNGEN cell types}
\usage{
score_cengen_clusters(
  markers,
  reference,
  top_n = 20,
  min_pct_expr = 10,
  combine = c("arithmetic", "geometric"),
  cov_weight = 0.5,
  spec_weight = 0.5,
  cutoff = 0.6,
  min_gap = 0.1,
  min_genes_used = 5,
  cluster_col = "cluster",
  gene_col = "gene"
)
}
\arguments{
\item{markers}{Either raw marker output (see \code{\link[=prepare_marker_panel]{prepare_marker_panel()}})
or an already-prepared panel (a data frame with \code{cluster}, \code{gene},
\code{rank}, \code{n_used} columns).}

\item{reference}{A \code{cengen_reference} object (see \code{\link[=new_cengen_reference]{new_cengen_reference()}}
/ \code{\link[=load_cengen_reference]{load_cengen_reference()}}).}

\item{top_n, cluster_col, gene_col}{Passed to \code{\link[=prepare_marker_panel]{prepare_marker_panel()}} when
\code{markers} is not already a prepared panel.}

\item{min_pct_expr}{Minimum percent-expressing (0-100) for a gene to
count as "covered" in a cell type; below this, coverage is 0.}

\item{combine}{How to combine the coverage and specificity signals per
gene: \code{"arithmetic"} (default) or \code{"geometric"}.}

\item{cov_weight, spec_weight}{Weights on the coverage and specificity
signals (exponents for \code{"geometric"}, linear weights for
\code{"arithmetic"}).}

\item{cutoff, min_gap, min_genes_used}{Passed to \code{\link[=classify_cengen_matches]{classify_cengen_matches()}}.}
}
\value{
A \code{cengen_scores} tibble, one row per cluster (see
\code{\link[=classify_cengen_matches]{classify_cengen_matches()}}), with the full score matrix attached as
an attribute.
}
\description{
Convenience wrapper that runs \code{\link[=score_cengen_matrix]{score_cengen_matrix()}} followed by
\code{\link[=classify_cengen_matches]{classify_cengen_matches()}}, and attaches the full cluster x cell-type
score matrix to the result (retrieve it with \code{\link[=cengen_matrix]{cengen_matrix()}}) for
drilling into all candidate cell types beyond the top 2.
}
