% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/seurat-glue.R
\name{write_cengen_annotations}
\alias{write_cengen_annotations}
\title{Write CeNGEN annotations onto a Seurat object}
\usage{
write_cengen_annotations(
  object,
  scores,
  cluster_col = "seurat_clusters",
  include = "annotate",
  metadata_col = "cengen_annotation",
  score_col = "cengen_annotation_score",
  overwrite = FALSE
)
}
\arguments{
\item{object}{A \code{Seurat} object.}

\item{scores}{A \code{cengen_scores} object, as returned by
\code{\link[=score_cengen_clusters]{score_cengen_clusters()}}.}

\item{cluster_col}{Name of the cluster identity column in
\code{object@meta.data} (default \code{"seurat_clusters"}).}

\item{include}{Which \code{decision} value(s) to write onto the object
(default \code{"annotate"} only).}

\item{metadata_col, score_col}{Names of the new metadata columns to add,
holding the matched cell type and its score respectively.}

\item{overwrite}{If \code{FALSE} (default), errors if \code{metadata_col} already
exists in \code{object@meta.data}.}
}
\value{
The modified \code{Seurat} object (not mutated in place, matching
Seurat's own convention).
}
\description{
Opt-in step that writes \code{\link[=score_cengen_clusters]{score_cengen_clusters()}} results onto a Seurat
object as per-cell metadata columns. Never called implicitly by the
scoring functions, and by default only writes clusters whose \code{decision}
is \code{"annotate"} — clusters sent to review stay unannotated (\code{NA}) unless
\code{include} is widened.
}
