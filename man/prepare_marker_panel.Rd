% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/markers.R
\name{prepare_marker_panel}
\alias{prepare_marker_panel}
\title{Prepare a per-cluster marker gene panel}
\usage{
prepare_marker_panel(
  markers,
  top_n = 20,
  cluster_col = "cluster",
  gene_col = "gene",
  p_val_adj_col = "p_val_adj",
  avg_log2FC_col = "avg_log2FC",
  max_p_val_adj = 0.05,
  only_pos = TRUE
)
}
\arguments{
\item{markers}{A data frame of per-cluster markers, e.g. the output of
\code{Seurat::FindAllMarkers()}.}

\item{top_n}{Number of top marker genes to keep per cluster (default 20).}

\item{cluster_col, gene_col}{Column names identifying the cluster and gene.}

\item{p_val_adj_col, avg_log2FC_col}{Column names used for filtering/ranking.
Set to \code{NULL} to skip the corresponding filter/ranking step.}

\item{max_p_val_adj}{Maximum adjusted p-value to keep. Ignored if
\code{p_val_adj_col} is \code{NULL}.}

\item{only_pos}{If \code{TRUE} (default), keep only genes with positive
\code{avg_log2FC} (upregulated markers). Ignored if \code{avg_log2FC_col} is \code{NULL}.}
}
\value{
A tibble with columns \code{cluster}, \code{gene}, \code{rank}, \code{n_requested},
\code{n_used} (the last two constant within a cluster; \code{n_used} is the
number of marker genes actually kept for that cluster, which may be
less than \code{top_n} if fewer significant markers were available).
}
\description{
Takes raw \code{\link[Seurat:FindAllMarkers]{Seurat::FindAllMarkers()}} output (or any per-cluster marker
\code{data.frame} with the same shape) and reduces it to the top marker genes
per cluster used for CeNGEN scoring. Fold-change and adjusted p-value are
used only to \emph{select} which genes make the panel; once selected, every
gene in the panel is treated equally by \code{\link[=score_cengen_matrix]{score_cengen_matrix()}}.
}
