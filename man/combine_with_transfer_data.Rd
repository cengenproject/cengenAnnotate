% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/combine-transfer-data.R
\name{combine_with_transfer_data}
\alias{combine_with_transfer_data}
\title{Combine cengenAnnotate results with Seurat TransferData predictions}
\usage{
combine_with_transfer_data(
  scores,
  object,
  cluster_col = "seurat_clusters",
  predicted_col = "predicted.id",
  score_col = "prediction.score.max",
  td_min_purity = 0.6,
  td_min_score = 0.6
)
}
\arguments{
\item{scores}{A \code{cengen_scores} object, as returned by
\code{\link[=score_cengen_clusters]{score_cengen_clusters()}}.}

\item{object}{A Seurat object with TransferData results already added
(i.e. has \code{predicted_col} and \code{score_col} metadata columns).}

\item{cluster_col}{Name of the cluster identity column in
\code{object@meta.data} (default \code{"seurat_clusters"}).}

\item{predicted_col, score_col}{Names of the per-cell TransferData
columns (Seurat's own defaults: \code{"predicted.id"},
\code{"prediction.score.max"}).}

\item{td_min_purity}{Minimum fraction of a cluster's cells that must
agree on TransferData's majority vote for that cluster to count as
TransferData-confident.}

\item{td_min_score}{Minimum mean per-cell \code{prediction.score.max} within
a cluster for it to count as TransferData-confident.}
}
\value{
A \code{cengen_scores} tibble (same shape as \code{scores}, with the score
matrix attribute preserved so \code{\link[=cengen_matrix]{cengen_matrix()}} still works), with
\code{decision} extended to include \code{"provisional"} and
\code{"review_conflict"}, \code{best_cell_type} updated to TransferData's call
for \code{"provisional"} rows, and three new diagnostic columns: \code{td_call},
\code{td_purity}, \code{td_mean_score}.
}
\description{
Augments a \code{cengen_scores} object (from \code{\link[=score_cengen_clusters]{score_cengen_clusters()}}) with
per-cluster predictions from \code{Seurat::TransferData()}, producing a
3-tier classification validated across two independent datasets:
clusters \code{cengenAnnotate} already confidently called stay \code{"annotate"};
clusters it didn't, but where TransferData's own per-cell votes are
confident, get promoted to \code{"provisional"} (labeled with TransferData's
call); everything else is left as-is for manual review.
}
\details{
On both test datasets, \code{cengenAnnotate}'s \code{"annotate"} calls and
TransferData's confident per-cell-vote majority never disagreed. A
genuine conflict (both methods confident, different answers) is
therefore treated as a rare edge case rather than silently resolved:
it's flagged as \code{"review_conflict"}, keeping \code{cengenAnnotate}'s own
(independently validated) call as \code{best_cell_type} while recording
TransferData's call in \code{td_call} for inspection.

\code{object} must already have \code{Seurat::TransferData()} results attached,
e.g.:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{anchors <- Seurat::FindTransferAnchors(reference = ref, query = object)
predictions <- Seurat::TransferData(anchorset = anchors, refdata = ref$cell_type)
object <- Seurat::AddMetaData(object, metadata = predictions)
}\if{html}{\out{</div>}}
}
